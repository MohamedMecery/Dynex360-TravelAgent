legacy-peer-deps=true
# Copy file: deps into node_modules instead of symlinking. @travelos/external-react
# must physically live under node_modules so its ESM "react" import escapes Next's
# react alias hook and resolves the same React copy @react-pdf/renderer uses
# (symlinked = hooked = every PDF endpoint fails with React error #31).
install-links=true
