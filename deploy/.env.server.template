# ============================================================================
# TravelOS — production .env for self-hosted VPS (srv1754043.hstgr.cloud)
# Copy to /docker/travelos/.env on the server and fill real secret values.
# chmod 600 after filling. NEVER commit the filled version.
# ============================================================================

NODE_ENV=production

# ── Public (also passed as Docker build args; inlined into client bundle) ────
NEXT_PUBLIC_SUPABASE_URL=https://ndomcfohwnvbyufnrxek.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_pQMEBIrPxYJAjHF0wtJGqw__s8czcSF
NEXT_PUBLIC_SITE_URL=https://travelos.srv1754043.hstgr.cloud

# ── Server-only secrets — NEVER expose to browser ────────────────────────────
SUPABASE_SERVICE_ROLE_KEY=__FILL_FROM_SUPABASE_DASHBOARD__

# Cron auth (used by n8n schedule trigger -> GET /api/cron/process-dispatch-jobs)
CRON_SECRET=__GENERATE_A_LONG_RANDOM_SECRET__

# ── Paymob (LIVE) ────────────────────────────────────────────────────────────
PAYMOB_MOCK_MODE=false
PAYMOB_MOCK_WEBHOOKS=false
PAYMOB_API_KEY=__FILL__
PAYMOB_INTEGRATION_ID=__FILL__
PAYMOB_IFRAME_ID=__FILL__
PAYMOB_HMAC_SECRET=__FILL__

# ── WhatsApp Meta Cloud API (LIVE) ───────────────────────────────────────────
WHATSAPP_MOCK_MODE=false
WHATSAPP_META_PHONE_NUMBER_ID=__FILL__
WHATSAPP_META_ACCESS_TOKEN=__FILL__
WHATSAPP_META_APP_SECRET=__FILL__
WHATSAPP_WEBHOOK_VERIFY_TOKEN=__FILL__
WHATSAPP_META_API_VERSION=v21.0

# ── Email (Resend, LIVE) ─────────────────────────────────────────────────────
EMAIL_PROVIDER=resend
RESEND_API_KEY=__FILL__
RESEND_FROM_EMAIL=noreply@yourdomain.com

# ── AI agents (server-only) ──────────────────────────────────────────────────
ANTHROPIC_API_KEY=__FILL__
OPENAI_API_KEY=__FILL__
