# TravelOS branding assets

- `travelos-logo.png` — optional default logo for invoice PDFs. When absent, PDFs render a styled TravelOS text mark.
- Tenant-specific logos may later be configured via `tenant_settings.settings.company_logo` (path under `/public`).
